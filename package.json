{
  "name": "muscle-leg-merge",
  "version": "0.1.0",
  "private": true,
  "description": "A dependency-free browser merge game about leveling up muscle legs.",
  "scripts": {
    "start": "node server.js",
    "start:lan": "HOST=0.0.0.0 node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
